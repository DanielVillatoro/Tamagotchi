mod constants;

mod systems {
    mod actions;
}

mod utils {
    mod random;
}

#[cfg(test)]
mod tests {
    mod test_random;
}

mod models {
    mod beast;
    mod beast_stats;
    mod beast_status;
}
